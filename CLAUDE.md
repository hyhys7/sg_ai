# CLAUDE.md - sg_ai

## Project Overview
This repository (`sg_ai`) contains AI and code projects.

Skills live under `.claude/skills/<name>/SKILL.md` and are distributed read-only via this public repo (see `README.md`): owner pushes, others only clone/pull.

## Commands
- Git push: `git add . && git commit -m "update" && git push`
